// background.js for Web Paint & Screenshot Tool

chrome.runtime.onInstalled.addListener(() => {
  // Optional: Show a welcome page or set up initial state
  // chrome.tabs.create({ url: "https://your-extension-welcome-page.com" });
  console.log("Web Paint & Screenshot Tool installed.");
});

// Placeholder for future background logic (e.g., keyboard shortcuts, persistent state)
